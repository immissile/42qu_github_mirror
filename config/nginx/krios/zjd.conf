

server {
	listen		80;
	server_name www.zjd.xxx;
	charset utf-8;
	rewrite ^(.*)$ http://zjd.xxx$1 permanent;
}
upstream zjd_xxx{
    server 127.0.0.1:30200 weight=1;
}
server {
	listen 80;
	server_name zjd.xxx *.zjd.xxx;
	location / {
		expires -1;
        proxy_set_header Host $host;
		proxy_pass http://zjd_xxx;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		access_log /var/log/nginx/zjd_xxx.access_log main;
		error_log /var/log/nginx/zjd_xxx.error_log info;
	}
}
server {
	listen 80;
	server_name api.zjd.xxx;

	location ~ ^/(favicon\.ico|crossdomain\.xml) {
		expires 24h;
		root /home/zjd/zpage/static;
	}
	location / {
		expires -1;
		proxy_pass http://127.0.0.1:30220;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		access_log /var/log/nginx/api_zjd_xxx.access_log main;
		error_log /var/log/nginx/api_zjd_xxx.error_log info;
	}
}
server {
	listen 80;
	server_name god.zjd.xxx;

	location ~ ^/(favicon\.ico|crossdomain\.xml) {
		expires 24h;
		root /home/zjd/zpage/static;
	}

	location ~ ^/(swf)/{
		autoindex off;
		expires 90d;
		root	/home/zjd/zpage/static;
	}
	location / {
		expires -1;
		proxy_pass http://127.0.0.1:30210;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		access_log /var/log/nginx/god_zjd_xxx.access_log main;
		error_log /var/log/nginx/god_zjd_xxx.error_log info;
	}
}
server {
	listen		80;
	server_name	s.zjd.xxx;
	charset utf-8;
    expires -1;
	access_log off;
	location / {
		#valid_referers none blocked 41dev.com 42qu.com *.42qu.com 42qu.org *.42qu.org *.qq.com qq.com google.com *.douban.com douban.com *.youdao.com youdao.com *.163.com 42qu.info *.42qu.info;
		autoindex off;
		root	/home/zjd/zpage/static;
	}
	location ~ ^/(css|js)/{
		autoindex off;
		expires 90d;
		root	/home/zjd/zpage/static;
		rewrite ^/js/([^~]*)~([^~]*)~(.*)$ /js/$1.$3 last;
		rewrite ^/css/([^~]*)~([^~]*)~(.*)$ /css/$1.$3 last;
	}
}

server {
	listen		80;
	server_name	p.zjd.xxx;
	charset utf-8;
    expires -1;
	access_log off;
	location / {
		autoindex off;
		root  /mnt/zpage;	
	}
}

